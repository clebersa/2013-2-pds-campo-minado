################################################################################
							TAREFAS CRÍTICAS
- Atualizar javadoc no que diz respeito à definição das classes. A definição
deve ser mais completa. Atualmente só fala que define atributos e métodos.

- TODA documentação JAVADOC precisa de revisão!!!

- A enum de TipoTabuleiro deve ser analizada. Caso seja permitido um tamanho 
personalizado de tabuleiro, a classe deve ser capaz de se adaptar a isso.

- Atualizar geração de builds para também executar os testes e outras atividades
 necessárias para o projeto. Isso deve equivaler ao makefile em C.

- Testar a implementação do método de inicialização do tabuleiro, na criação dos
quadrados.


##### Samuel #####
+ Implementação
	- Corrigir a implementação de distribuirMinas() em DistribuiAleatorio.java.
Caso tente-se adicioanr uma mina em um lugar que já tenha uma, deve-se sortear
outro lugar para a mesma ser colocada. Essa informação já é retornada pelo
método adicionarMina de tabuleiro, bastando assim, só tratar a informação. -- OK

	- Corrigir a implementação de DistribuiPorArquivo.java. Deve-se abrir o
arquivo indicado pelo caminho e nome de arquivos indicados dentro da própria
classe. Atualmente tenta-se abrir uma arquivo "mapadasminas". -- OK

	- Implementar a classe principal (CampoMinado.java, em model). Eu (Cleber) 
tentei executar a mesma dia 17/10/13 e ela apresentou NullPointerException
para qualquer um dos tabuleiros informados.

	- Implementar parte do método executarJogada(), de tabuleiro, onde deve-se 
verificar se o cara venceu.

+ Teste
	- Criar testes para DistribuiAleatorio.java --  OK
	- Criar testes para DistribuiPorArquivo.java --  OK
	- Criar testes para BandeirasIterator.java -- OK 
	- Criar testes para MinasIterator.java -- OK
	- Criar testes para TabuleiroIterator.java -- OK


################################################################################
							PERGUNTAS PARA O PROFESSOR
- Um quadrado não sabe qual é a linha e coluna na qual ele se encontra.
Entretanto, isso gera um certo problema: sempre que eu vou trabalhar nos
iteradores, eu acabo ficando dependente dessa linha e coluna para retornar dados
corretos para os objetos que chamam esses iteradores. Sendo assim, analisando 
como o projeto está estruturado e implementado, não seria melhor permitir que os
dados de linha e coluna?
	

################################################################################
							SUGESTÕES/PRIORIDADE BAIXA
- Tratar eventuais erros na leitura do arquivo da distribuição de minas. Assim,
o método de distribuição deverá lançar alguma(s) exceções caso erros ocorram.

- Adicionar temporizador, pois os recordes são dados através dele.

- Permitir que a quantidade de minas, linhas e colunas seja personalizável.

- Implementar interface gráfica.
