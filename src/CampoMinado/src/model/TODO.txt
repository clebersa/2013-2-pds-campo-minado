- Verificar as classes com os atributos e fazer com que elas tenham os métdos
que cumprem a função de get e set para permitir que a obtenção e definição de 
cada atributo seja feita através desses métodos.

- Verificar se é necessário que as classes principais das interfaces precisam
ser mantidas no projeto. Ex.: ExibivelInterface.

- Implementar a distribuição das minas.

- Implementar a classe Tabuleiro.

- Implementar a classe Principal.

- Verificar se na classe principal (CampoMinado.java) o ideal é utilizar 
composição ou agregação, ou seja, deixar como está ou não ter atributos globais.

- Atualizar javadoc no que diz respeito à definição das classes. A definição
deve ser mais completa. Atualmente só fala que define atributos e métodos.

- Criar testes para get e set?