- Implementar a classe Tabuleiro.

- Implementar a classe Principal.

- Atualizar javadoc no que diz respeito à definição das classes. A definição
deve ser mais completa. Atualmente só fala que define atributos e métodos.

- A enum de TipoTabuleiro deve ser analizada. Caso seja permitido um tamanho 
personalizado de tabuleiro, a classe deve ser capaz de se adaptar a isso.

- Atualizar geração de builds para também executar os testes e outras atividades
 necessárias para o projeto. Isso deve equivaler ao makefile em C.

- Falta para o samuel:
	- Terminar de implementar o método construtor de Tabuleiro. O método ainda
	tem que contabilizar as minas vizinhas.
	- Implementar contabilizarMinasVizinhasQuadrado() de Tabuleiro
	- Implementar exibir de Tabuleiro()
	- Corrigir a implementação de adicionarMina() em Tabuleiro. O método deve
	tratar quando o quadrado onde a mina está sendo adicionada já contiver uma mina.
	O método retorna TRUE quando a mina é instalada com sucesso e FALSE quando
	já existe uma mina onde a mesma foi adicionada.
	- Corrigir a implementação de distribuirMinas() nos distribuidores para
	tratar as mudança na implementação acima.
	- Remover função getTipoTabuleiro() de tabuleiro. Não será necessário utilizar a mesma.